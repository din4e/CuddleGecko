server:
  port: 8080
  mode: debug

database:
  driver: sqlite
  sqlite_path: ./data/cuddlegecko.db
  mysql_dsn: ""

jwt:
  secret: "change-me-in-production-please-use-at-least-32-chars"
  access_ttl: 15m
  refresh_ttl: 168h

log:
  level: info
  format: json

captcha:
  enabled: false

cors:
  allow_origins:
    - "http://localhost:5173"
    - "http://localhost:3000"
